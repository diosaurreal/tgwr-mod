foreign_politics = {
	SWI_surrender_to_FRA_or_FRC = {
		allowed = {
			original_tag = SWI
		}
		available = {
			any_enemy_country = {
				original_tag = FRA
			}
		}
		visible = {
			any_enemy_country = {
				original_tag = FRA
			}
			has_country_flag = SWI_FRA_war_for_upper_savoy
		}
		on_map_mode = map_and_decisions_view
		state_target = yes
		target_trigger = {
			FROM = {
				state = 735
			}
		}
		cost = 0
		complete_effect = {
			add_political_power = -50
			every_enemy_country = {
				limit = {
					OR = {
						original_tag = FRA
						has_war_together_with = FRC
						has_war_together_with = FRA
					}
				}
				white_peace = SWI
			}
			news_event = ww1_switzerland.12
		}
		ai_will_do = {
			factor = 50
			modifier = {
				surrender_progress < 0.2 #we are losing
				longest_war_length < 6 #we wont fight for long
				factor = 0
			}
			modifier = {
				longest_war_length < 6 #we wont fight for long
				NOT = {
					controls_state = 735 #we are not even controlling the objective
				}
				factor = 0
			}
		}
	}
}