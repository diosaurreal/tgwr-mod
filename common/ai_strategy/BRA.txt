BRA_dont_protect_france = {
	allowed = {
		tag = BRA
	}
	enable = {
		is_in_faction_with = FRA
		FRA = {
			surrender_progress < 0.5
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = dont_defend_ally_borders
		id = FRA
		value = 1
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		id = BEL
		value = 1
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		id = LUX
		value = 1
	}
}
no_alliance_with_BRA = {
	allowed = {
		is_latin_american_country = no
	}
	enable = {
		BRA = {
			is_faction_leader = yes
		}	
	}
	abort_when_not_enabled = yes	
	ai_strategy = {
		type = alliance
		id = BRA
		value = -200
	}
}	

BRA_ignore_PRU = { #to not die in the Amazons
	allowed = {
		original_tag = BRA
	}
	enable = {
		is_neighbor_of = PRU
	}
	abort = {
		OR = {
			is_ally_with = PRU
			has_war_with = PRU
			OR = {
				is_justifying_wargoal_against = PRU
				has_wargoal_against = PRU
			}
			PRU = {
				OR = {
					is_justifying_wargoal_against = ROOT
					has_wargoal_against = ROOT
				}
			}
		}
	}
	ai_strategy = {
		type = front_unit_request
		tag = PRU
		value = -150
	}
}