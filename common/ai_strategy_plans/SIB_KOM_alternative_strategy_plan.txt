SIB_KOM_leftist_plan = {
	name = "SIB KOM leftist plan"
	desc = ""

	allowed = {
		OR = {
			tag = SIB
			tag = KOM
		}
	}
	enable = {
		OR = {
			tag = KOM			
			has_game_rule = {
				rule = SIB_ai_behavior 
				option = LEFTIST
			}
			has_country_flag = AI_COALITION 
			has_game_rule = {
				rule = SIB_ai_behavior 
				option = COALITION
			}
			has_country_flag = AI_ESER 
			has_game_rule = {
				rule = SIB_ai_behavior 
				option = ESER
			}
		}
	}
	ai_national_focuses = {
		SIB_recognize_alash_autonomy
		SIB_root_out_the_monarchists
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
	}
}

SIB_KOM_coalition_plan = {
	name = "SIB KOM coalition plan"
	desc = ""

	allowed = {
		OR = {
			tag = SIB
			tag = KOM
		}
	}
	enable = {
		OR = {	
			has_country_flag = AI_COALITION
			has_completed_focus = SIB_progressive_coalition
			AND = {
				tag = SIB
				has_game_rule = {
					rule = SIB_ai_behavior 
					option = COALITION
				}
			}
			AND = {
				tag = KOM
				has_game_rule = {
					rule = KOM_ai_behavior 
					option = COALITION
				}
			}		
		}
	}

	ai_national_focuses = {
		SIB_progressive_coalition
		SIB_depolitization_of_the_army
		SIB_mobilize_the_economy
		SIB_contact_the_white_forces
		SIB_legalize_black_partition
		SIB_support_from_entente
		SIB_federative_republic
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
	}
}

SIB_KOM_eser_plan = {
	name = "SIB KOM Eser plan"
	desc = ""

	allowed = {
		OR = {
			tag = SIB
			tag = KOM
		}
	}
	enable = {
		OR = {	
			has_country_flag = AI_ESER
			has_completed_focus = SIB_no_compromise_with_reaction
			AND = {
				tag = SIB
				has_game_rule = {
					rule = SIB_ai_behavior 
					option = ESER
				}
			}
			AND = {
				tag = KOM
				has_game_rule = {
					rule = KOM_ai_behavior 
					option = ESER
				}
			}		
		}
	}
	ai_national_focuses = {
		SIB_no_compromise_with_reaction
		SIB_redemocratization_of_the_army
		SIB_workers_control
		SIB_finish_the_whites_off
		SIB_land_to_the_peasants
		SIB_volunteers_of_the_constituent_assembly
		SIB_federative_republic
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
	}
}