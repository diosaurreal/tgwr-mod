SIB_pre_unification_plan = {
	name = "Siberian pre-unification plan"
	desc = ""

	allowed = {
		tag = SIB
	}

	enable = {
		always = yes
	}

	abort = {
		has_game_rule = {
			rule = SIB_KOM_ai_unification
			option = SIBERIA
		}
	}

	ai_national_focuses = {
		SIB_proclaim_siberia
		SIB_mobilization_of_the_siberian_army
		SIB_question_of_regional_duma
		SIB_restore_all_russian_government
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
	}
}

KOM_pre_unification_plan = {
	name = "Komuch pre-unification plan"
	desc = ""

	allowed = {
		tag = KOM
	}

	enable = {
		always = yes
	}

	ai_national_focuses = {
		KOM_consolidate_power
		KOM_mobilisation_of_the_peoples_army
		KOM_form_the_governing_council
		KOM_contact_anti_bolshevik_forces
		SIB_restore_all_russian_government
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
	}
}



SIB_historical_plan = {
	name = "Siberian historical plan"
	desc = ""

	allowed = {
		tag = SIB
	}
	enable = {
		tag = SIB
		OR = {
			AND = {
				is_historical_focus_on = yes
				has_game_rule = {
					rule = SIB_ai_behavior 
					option = DEFAULT
				}
			}
			has_country_flag = SIB_AI_HISTORICAL
			has_game_rule = {
				rule = SIB_ai_behavior 
				option = HISTORICAL
			}
			has_completed_focus = SIB_a_strong_hand
		}
	}
    abort = {
    }

	ai_national_focuses = {
		SIB_solidify_control
		SIB_a_strong_hand
		SIB_lobby_for_foreign_aid
		SIB_form_russian_army
		SIB_emergency_economic_council
		SIB_create_omon
		SIB_restore_railroads
		SIB_demand_recognition_from_generals
		SIB_national_constituent_assembly
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}
}

SIB_KOM_leftist_plan = {
	name = "KOM leftist rical plan"
	desc = ""

	allowed = {
		tag = SIB
	}
	enable = {
		tag = SIB
		OR = {
			AND = {
				is_historical_focus_on = yes
				has_game_rule = {
					rule = SIB_ai_behavior 
					option = DEFAULT
				}
			}
			has_country_flag = SIB_AI_HISTORICAL
			has_game_rule = {
				rule = RUS_ai_behavior 
				option = HISTORICAL
			}
			has_completed_focus = SIB_a_strong_hand
		}
	}
    abort = {
    }

	ai_national_focuses = {
		SIB_recognize_alash_autonomy
		SIB_root_out_the_monarchists
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
	}
}