LBA_motus_vivendi = {
	set_country_flag = LBA_modus_vivendi_of_acroma
	every_enemy_country = {
		limit = {
			has_country_flag = ITA_LBA_war_in_desert
		}
		set_country_flag = LBA_modus_vivendi_of_acroma
	}
	hidden_effect = {
		if = {
			limit = {
				has_capitulated = yes
			}
			#prevent generic peace
			set_state_controller = 810
			set_state_controller = 1121
		}
	}
	if = {
		limit = { is_in_faction = yes }
		leave_faction = yes
	}
	add_ideas = standing_alone
	every_state = {
		limit = {
			owner = { has_war_with = ROOT }
		}
		if = {
			limit = {
				OR = {
					state = 810
					state = 817
					state = 1121
					state = 754
				}
			}
			transfer_state_to = ROOT
		}
		if = {
			limit = {
				state = 1060
			}
			transfer_state_to = TRP
		}
		if = {
			limit = { 
				is_controlled_by = ROOT
				is_core_of = ROOT
				NOT = {
					state = 552
					state = 1131
					state = 450
					state = 448
				}
				impassable = no
			}
			set_state_controller_to = OWNER #reseting the desert transfer
			transfer_state_to = ROOT
		}
	}
	white_peace = FROM
	clr_country_flag = ITA_LBA_war_in_desert
	hidden_effect = {
		every_enemy_country = {
			limit = {
				is_in_faction_with = FROM
			}
			ROOT = {
				white_peace = PREV
			}
		}
		if = {
			limit = {
				country_exists = TRP
			}
			set_truce = {
				target = TRP
				days = 365
			}
			diplomatic_relation = {
				country = TRP
				relation = non_aggression_pact
				active = yes
			}
		}
	}
	hidden_effect = {
		every_owned_state = {
			limit = {
				is_claimed_by = ENG
				is_core_of = EGY
			}
			transfer_state_to = ENG	
		}
		if = {
			limit = {
				EGY = {
					OR = {
						is_in_faction_with = ENG
						is_subject_of = ENG
						has_war_together_with = ENG
					}
				}
			}		
			every_owned_state = {
				limit = {
					is_core_of = EGY
				}
				transfer_state_to = EGY
			}
		}
		if	= {
			limit = {
				1135 = {
					OWNER = {
						OR = {
							tag = FRA
							is_subject_of = FRA
						}
					}				
				}		
			}
			every_owned_state = {
				limit = {
					is_claimed_by = FRA
					is_core_of = CHA
				}
				1135 = {
					OWNER = {
						transfer_state = PREV.PREV
					}	
				}		
			}
		}
		every_state = {
			limit = {
				NOT = {
					is_owned_by = ROOT
					state = 1134
				}
				OR = {
					is_core_of = EGY
					is_core_of = CHA
					is_core_of = SUD
				}
			}
			remove_core_of = ROOT
		}
		every_state = {
			limit = {
				is_claimed_by = ROOT
			}
			remove_claim_by = ROOT
		}
	}
}