RCW_core_states_valid_targer = {
	is_controlled_by = ROOT
	NOT = { is_core_of = ROOT }
	OWNER = {
		OR = {
			tag = ROOT
			AND = {
				has_war_with = ROOT
				if = {
					limit = { ROOT = { tag = SOV } }
					is_cossack_tag = no
				}
			}		
		}
	}
}