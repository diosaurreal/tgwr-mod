TUR_sabotage_the_suez_canal = {
	icon = GFX_operations_targeted_sabotage
	map_icon = GFX_operations_targeted_sabotage_map
	name = TUR_sabotage_the_suez_canal
	desc = TUR_sabotage_the_suez_canal_desc
	days = 70

	priority = 11

	allowed = { original_tag = TUR }
	available = { FROM = { has_war_with = ROOT } }
	visible = {
		has_completed_focus = TUR_empower_the_special_organization
		OR = {
			has_country_leader = { character = TUR_ismail_enver }
			TUR_ismail_enver = { is_hired_as_advisor = yes }
		}
		FROM = { controls_state = 447 }
	}

	operatives = 2

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 50

	equipment = {
		convoy = 30
		support_equipment = 300
	}

	on_start = {  # SCOPE_OPERATION
		
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	risk_chance = 0.5
	experience = 5
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		set_global_flag = SUEZ_CANAL_BLOCKED
		set_global_flag = SUEZ_CANAL_BLOCKED_but_not_serious
		ROOT = { news_event = ottoman.222 }
	}
	outcome_potential = {
		set_global_flag = SUEZ_CANAL_BLOCKED
		set_global_flag = SUEZ_CANAL_BLOCKED_but_not_serious
		ROOT = { news_event = ottoman.222 }
	}

	# This is where all the separate phases are defined
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration
		infiltration_border = { base = 50 }
		infiltration_submarine = { base = 50 }
	}
	phases = { #exfiltration
		targeted_sabotage_plant_explosives = { base = 100 }
	}
	phases = { #exfiltration
		exfiltration_border = { base = 25 }
		exfiltration_go_to_ground = { base = 35 }
		exfiltration_submarine_pickup = { base = 40 }
	}
}

TUR_steal_classified_documents = {
	icon = GFX_operations_capture_ciphers
	map_icon = GFX_operations_capture_ciphers_map
	name = operation_capture_cipher
	desc = operation_capture_cipher_desc
	priority = 3

	days = 70
	network_strength = 40
	operatives = 1

	visible = {
		has_completed_focus = TUR_officialize_the_second_bureau
		OR = {
			has_country_leader = { character = TUR_miralay_sadik }
			TUR_miralay_sadik = { is_hired_as_advisor = yes }
		}
		FROM = { is_major = yes }
	}

	equipment = {
		
	}
	risk_chance = 0.25
	outcome_execute = {
		ROOT = {
			custom_effect_tooltip = TUR_steal_classified_documents_tt
			hidden_effect = {
				if = {
					limit = { has_variable = TUR_stolen_documents }
					FROM = { add_to_variable = { TUR_stolen_documents = 1 } }
					add_to_variable = { TUR_stolen_documents_ROOT = 1 }
				}
				else = {
					FROM = { set_variable = { TUR_stolen_documents = 1 } }
					set_variable = { TUR_stolen_documents_ROOT = 1 }
				}
			}
		}
	}

	outcome_potential = {
		ROOT = {
			custom_effect_tooltip = TUR_steal_classified_documents_tt
			hidden_effect = {
				if = {
					limit = { has_variable = TUR_stolen_documents }
					FROM = { add_to_variable = { TUR_stolen_documents = 1 } }
					add_to_variable = { TUR_stolen_documents_ROOT = 1 }
				}
				else = {
					FROM = { set_variable = { TUR_stolen_documents = 1 } }
					set_variable = { TUR_stolen_documents_ROOT = 1 }
				}
			}
		}
	}

	phases = { #infiltration
		infiltration_border = { base = 25 }
		infiltration_diplomatic = {
			base = 25
			modifier = {
				factor = 20
				ROOT = { has_war = no }
				FROM = {
					has_war = no
				}
			}
		}
	}
	phases = { #infiltrate
		infiltrate_military_bribe = { base = 25 }
		infiltrate_military_seduction = { base = 25 }
	}

	phases = { #exfiltration
		exfiltration_border = { base = 30 }
		exfiltration_go_to_ground = { base = 30 }
	}
}

TRS_increase_egyptian_opposition = {
	icon = GFX_operations_boost_resistance
	map_icon = GFX_operations_boost_resistance_map
	name = TRS_increase_egyptian_opposition
	desc = TRS_increase_egyptian_opposition_desc
	days = 35

	priority = 11

	allowed = { tag = TRS }
	available = {
        FROM = {
            has_war_with = ROOT
            if = {
				limit = { NOT = { has_idea = TRS_pro_ottoman_opposition_very_low } }
				fascism > 0.05
			}
			else_if = {
				limit = { NOT = { has_idea = TRS_pro_ottoman_opposition_low } }
				fascism > 0.1
			}
			else_if = {
				limit = { NOT = { has_idea = TRS_pro_ottoman_opposition_medium } }
				fascism > 0.15
			}
			else_if = {
				limit = { NOT = { has_idea = TRS_pro_ottoman_opposition_high } }
				fascism > 0.2
			}
			else_if = {
				limit = { NOT = { has_idea = TRS_pro_ottoman_opposition_very_high } }
				fascism > 0.25
			}
			else_if = {
				limit = { NOT = { has_idea = TRS_pro_ottoman_opposition_super_high } }
				fascism > 0.3
			}
        }
	}
	visible = {
		has_completed_focus = TUR_popular_egyptian_resistance
		FROM = {
			tag = EGY
            NOT = { has_government = fascism }
            NOT = { has_idea = TRS_pro_ottoman_opposition_super_high }
        }
	}

	operatives = 1

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 30

	equipment = {
		support_equipment = 30
		infantry_equipment = 300
	}

	on_start = {  # SCOPE_OPERATION
		
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	risk_chance = 0.15
	experience = 0.5
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		FROM = {
			event_option_tooltip = ottoman_syria.9.a
			hidden_effect = {  country_event = ottoman_syria.9 }
		}
	}
	outcome_potential = {
		FROM = {
			event_option_tooltip = ottoman_syria.9.a
			hidden_effect = {  country_event = ottoman_syria.9 }
		}
	}

	# This is where all the separate phases are defined
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration 
		infiltration_border = { base = 100 }
	}
	phases = {
		resistance_support_arms_depots = { base = 50 }
		resistance_support_distribute_weapons = { base = 50 }
	}
	phases = {
		exfiltration_border = { base = 100 } 
	}
}

TUR_start_the_egyptian_uprising = {
	icon = GFX_operations_coup_government
	map_icon = GFX_operations_coup_government_map
	name = TUR_start_the_egyptian_uprising
	desc = TUR_start_the_egyptian_uprising_desc
	days = 70

	priority = 11

	allowed = { tag = TRS }
	available = { FROM = { has_war_with = ROOT } }
	visible = {
		has_completed_focus = TUR_instigate_egyptian_rebellion
		NOT = { has_global_flag = TRS_egyptian_uprising }
		FROM = {
			tag = EGY
            NOT = { has_government = fascism }
        }
	}

	operatives = 1

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 30

	equipment = {
		support_equipment = 50
		infantry_equipment = 500
	}

	on_start = {  # SCOPE_OPERATION
		
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	risk_chance = 0.15
	experience = 2
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		FROM = { country_event = ottoman_syria.15 }
	}
	outcome_potential = {
		FROM = {
			country_event = ottoman_syria.15
			custom_effect_tooltip = ottoman_syria.15.tt
		}
	}

	# This is where all the separate phases are defined
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration 
		infiltration_border = { base = 100 }
	}
	phases = {
		resistance_support_arms_depots = { base = 25 }
		resistance_support_distribute_weapons = { base = 25 }
		resistance_support_organize_training = { base = 25 }
		resistance_support_coordinate_groups = { base = 25 }
	}
	phases = {
		exfiltration_border = { base = 100 } 
	}
}
